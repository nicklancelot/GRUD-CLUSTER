# Frontend env example for dev/prod
# In development Vite proxy uses /api -> http://localhost:5000
# For production set the full backend URL (including /api path if desired)
VITE_API_BASE_URL=http://104.197.35.63:5000/api

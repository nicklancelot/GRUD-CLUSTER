# Backend Express + MongoDB

## Installation

1. Copier `.env.example` vers `.env`
2. Remplacer `DB_PASSWORD` par votre vrai mot de passe MongoDB
3. Installer les dependances
4. Lancer le serveur

```bash
npm install
npm run dev
```

Le backend expose l'API sur `http://localhost:5000/api`.
